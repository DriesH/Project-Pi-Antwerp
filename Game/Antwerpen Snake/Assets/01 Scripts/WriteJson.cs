using UnityEngine;
using System.Collections;
using System.IO;
using LitJson;

public class WriteJson : MonoBehaviour {
  
  private string qData = "Test";
  JsonData answerData;

	void Start () {
  //  answerData = JsonMapper.ToJson(qData);
   // File.WriteAllText(Application.dataPath + "/06 Resources/writeJson.json", answerData.ToString());

  //  WWWForm form = new WWWForm();


      //postdata: A byte array of data to be posted to the url.
      //headers 	A dictionary that contains the header keys and values to pass to the server.
	}

}